---
description: Install the package in development mode
allowed-tools: Bash
---

Install omelet-cli in development mode:

```bash
pip install -e .
```

Then verify the installation:

```bash
omelet --help
```

Report if the installation was successful and show the available commands.
